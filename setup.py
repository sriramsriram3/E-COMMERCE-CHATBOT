from setuptools import setup,find_packages

setup(name="flipkart",
      version='0.0.1',
      description='it is a function used for flipkart recommendation system',
      author='simhadrisriram',
      author_email='simhadrisriram3@gmail.com',
      packages=find_packages())